<h4>
            <?php 
            // Verificamos si la variable $mensaje está definida y no es null
            if (isset($mensaje)) 
                // Si $mensaje está definido, lo mostramos en el encabezado
                echo $mensaje; 
            ?> 
        </h4>