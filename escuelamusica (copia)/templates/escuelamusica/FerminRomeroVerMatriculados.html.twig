{% extends 'base.html.twig' %}

{% block title %}Escuela de Música
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="container">

		<h4>{{ titulo }}{{ app.user.username }}</h4>

		<table class="table table-striped table-hover">
			<tr>
				<th>ID Alumno</th>
				<th>Nombre Alumno matriculado</th>
			</tr>
			{% for e in matriculas %}
                <tr>
					<td>{{ e.id}}</td>
					<td>{{ e.alumno.username }}</td>
                </tr>
            {% endfor %}


		</td>

	</tr>
</table></div>{% endblock %}
